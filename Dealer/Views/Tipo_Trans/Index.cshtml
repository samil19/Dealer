@model IEnumerable<Dealer.Tipo_Trans>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tipo_Trans1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tipo_Trans1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_TipoTrans }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_TipoTrans }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_TipoTrans })
        </td>
    </tr>
}

</table>
